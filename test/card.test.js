"use strict";

var should = require('should');
var helper = require('./helper');
var Card = helper.pagarMe.card;
var _ = require('lodash');
var info = {};

describe('Card', function() {
  it('should create a new card', function(done) {
    console.log('   TODO');
    done();
  });

  it('should return a card', function(done) {
    console.log('   TODO');
    done();
  });
});