"use strict";

var should = require('should');
var helper = require('./helper');
var Card = helper.pagarMe.plan;
var _ = require('lodash');
var info = {};

describe('#Plan', function() {
  it('should create a plan', function(done) {
    console.log('			TODO');
    done();
  });

  it('should return all plans', function (done) {
    console.log(' 	  TODO');
    done();  	
  });

  it('should return a plan', function(done) {
    console.log('	   TODO');
    done();
  });
});